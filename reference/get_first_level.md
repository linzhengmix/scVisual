# Get first level maintaining factor order

Get first level preserving factor order if factor, otherwise first
unique value. This ensures consistent behavior for corner axes placement
and other ordered operations.

## Usage

``` r
get_first_level(x)
```

## Arguments

- x:

  A vector to process

## Value

First level as factor if original was factor, otherwise unique value
